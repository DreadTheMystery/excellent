<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Online Admissions | Excellence Academy</title>
  <link rel="stylesheet" href="/styles.css" />
</head>

<body>
  <main class="container section">
    <div class="panel">
      <h2>Online Admission Form</h2>
      <p class="note">Fields marked * are required.</p>
      <% if (error) { %>
        <div class="alert error">
          <%= error %>
        </div>
        <% } %>
          <% if (success) { %>
            <div class="alert success">Your application has been received. We will contact you shortly.</div>
            <% } %>
              <form action="/admissions" method="POST" class="form grid-form">
                <input type="text" name="student_name" placeholder="Student Full Name *" required />
                <input type="date" name="dob" required />
                <select name="gender" required>
                  <option value="">Gender *</option>
                  <option value="Male">Male</option>
                  <option value="Female">Female</option>
                </select>
                <select name="class_applied" required>
                  <option value="">Class Applying For *</option>
                  <% classOptions.forEach((className)=> { %>
                    <option value="<%= className %>">
                      <%= className %>
                    </option>
                    <% }) %>
                </select>
                <input type="text" name="parent_name" placeholder="Parent/Guardian Name *" required />
                <input type="tel" name="parent_phone" placeholder="Parent Phone *" required />
                <input type="email" name="parent_email" placeholder="Parent Email *" required />
                <input type="text" name="address" placeholder="Home Address" />
                <input type="text" name="previous_school" placeholder="Previous School" />
                <textarea name="notes" rows="4" placeholder="Additional Notes"></textarea>
                <button class="btn" type="submit">Submit Application</button>
              </form>
    </div>
  </main>
</body>

</html>